using UnityEngine;

// Componente marcador (marker).
// Serve apenas para identificar que este GameObject é um Goal.
// O LevelManager, GoalHighlighter, validadores de vitória e HUD
// podem localizar Goals buscando por este componente.
public class GoalIdentifier : MonoBehaviour
{
    // Nenhuma lógica aqui — só identificação.
}